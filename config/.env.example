# Telegram Media Hub - 环境变量配置说明

## 配置文件位置
`.env` 文件应该放置在 `config/` 目录下。

## 必需配置项

### Telegram API 凭据
```bash
TG_API_ID=your_api_id              # 从 https://my.telegram.org 获取
TG_API_HASH=your_api_hash          # 从 https://my.telegram.org 获取
TG_SESSION_NAME=media_hub          # Session 名称（推荐保持默认）
```

**如何获取 Telegram API 凭据：**
1. 访问 https://my.telegram.org
2. 登录你的 Telegram 账号
3. 进入 "API development tools"
4. 创建新应用，获取 API ID 和 API Hash
5. **务必保管好这些凭据，不要泄露！**

### 数据库连接
```bash
DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/media_hub
```

**部署说明：**
- 本地开发：使用 `localhost:5432`
- Docker 环境：使用服务名 `postgres:5432`

### Redis 连接
```bash
REDIS_URL=redis://redis:6379/0
```

**部署说明：**
- 本地开发：使用 `localhost:6379`
- Docker 环境：使用 `redis:6379`

## 可选配置项

### MeTube URL
```bash
METUBE_URL=http://metube:8081
```

MeTube 是可选的 Web UI for yt-dlp，如不需要可保持默认。

### 存储路径
```bash
STORAGE_ROOT=/media
TEMP_DIR=/media/temp
```

- `STORAGE_ROOT`: 下载文件的根目录
- `TEMP_DIR`: 临时文件目录（下载中）

### 重试配置
```bash
MAX_RETRIES=5
RETRY_BASE_DELAY=30
```

- `MAX_RETRIES`: 最大重试次数
- `RETRY_BASE_DELAY`: 初始重试延迟（秒），使用指数退避

### 代理池配置
```bash
PROXY_POOL=http://proxy1:7890,socks5://proxy2:1080
```

逗号分隔的代理列表，支持 HTTP 和 SOCKS5。

### yt-dlp 配置
```bash
YTDLP_FORMAT=bestvideo+bestaudio/best
YTDLP_USE_ARIA2=true
```

- `YTDLP_FORMAT`: 下载格式选择
- `YTDLP_USE_ARIA2`: 是否使用 aria2 分片下载

### 通知配置
```bash
TG_NOTIFY_ON_COMPLETE=true
TG_NOTIFY_ON_FAIL=true
```

- `TG_NOTIFY_ON_COMPLETE`: 下载完成后在TG聊天中回复
- `TG_NOTIFY_ON_FAIL`: 下载失败时在TG聊天中回复

## 完整示例

```bash
# 必需项
TG_API_ID=12345678
TG_API_HASH=abcdef1234567890abcdef1234567890
TG_SESSION_NAME=media_hub

# 数据库和Redis
DATABASE_URL=postgresql+asyncpg://postgres:postgres@postgres:5432/media_hub
REDIS_URL=redis://redis:6379/0

# 存储路径
STORAGE_ROOT=/media
TEMP_DIR=/media/temp

# 重试配置
MAX_RETRIES=5
RETRY_BASE_DELAY=30

# 代理（可选）
# PROXY_POOL=http://localhost:7890

# yt-dlp配置
YTDLP_FORMAT=bestvideo+bestaudio/best
YTDLP_USE_ARIA2=true

# 通知
TG_NOTIFY_ON_COMPLETE=true
TG_NOTIFY_ON_FAIL=true
```

## 创建 .env 文件

### 方法1：复制 .env.example
```bash
cp config/.env.example config/.env
```

然后编辑 `config/.env` 填写你的实际配置。

### 方法2：手动创建
```bash
touch config/.env
# 然后使用文本编辑器添加上述配置项
```

## 安全注意事项

⚠️ **重要：**
1. `.env` 文件已添加到 `.gitignore`，**不要提交到Git仓库**
2. Telegram API Hash 是敏感信息，不要分享
3. 如果凭据泄露，请立即在 https://my.telegram.org 重新生成
4. 使用强密码保护数据库和Redis
5. 在生产环境中，考虑使用环境变量或密钥管理服务

## 生产环境部署建议

1. **使用独立的数据库用户**，而不是root
2. **启用SSL连接**数据库
3. **使用Secrets管理**（如Docker Secrets、Kubernetes Secrets）
4. **定期备份**数据库和下载文件
5. **监控日志**和任务状态

## 故障排查

### 问题：无法连接数据库
```bash
# 检查DATABASE_URL格式
# 确认数据库服务正在运行
# Docker: 检查 postgres 服务是否 healthy
```

### 问题：Session文件缺失
```bash
# 通过Web UI重新登录 Telegram
# 访问 http://localhost:8000 -> TG Login tab
```

### 问题：代理无法使用
```bash
# 检查代理URL格式
# 使用 Web UI 的 Proxy Test 功能测试代理
```

---

**最后更新：** 2026-04-24
